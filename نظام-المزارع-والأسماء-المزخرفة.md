# نظام إشعارات المزارع والأسماء المزخرفة

## نظرة عامة
تم إضافة نظامين جديدين للعبة:
1. **نظام إشعارات المزارع** - يرسل تنبيهات للاعبين عن حالة مزارعهم
2. **نظام الأسماء المزخرفة** - يمكن اللاعبين من كتابة أسماءهم بشكل مزخرف

## الميزات المضافة

### 🌾 نظام إشعارات المزارع
- **المراقبة التلقائية**: يفحص حالة المزارع كل دقيقة
- **إشعارات فورية**: يرسل تنبيهات فورية عند اكتمال الإنتاج
- **تنبيهات الصيانة**: يحذر من المزارع التي تحتاج صيانة
- **ربط صوتي**: يشغل أصوات مناسبة لكل إشعار
- **عرض في الشريط المتحرك**: يضيف الإشعارات للشريط المتحرك

### ✨ نظام الأسماء المزخرفة
- **خطوط متنوعة**: مزخرف، عريض، مخطوط، قوطي
- **رموز زخرفية**: تاج ملكي، نجمة، برق، إلخ
- **زخارف**: إطارات، أقواس، فواصل
- **حفظ تلقائي**: يحفظ الاسم المزخرف في التخزين المحلي
- **معاينة فورية**: يظهر النتيجة فوراً أثناء الكتابة

### 🎵 النظام الصوتي المطور
- **متكامل مع المزارع**: أصوات للإشعارات المختلفة
- **تحكم كامل**: مستوى صوت الموسيقى والتأثيرات
- **اختبار الأصوات**: أزرار لاختبار كل صوت
- **إعدادات محفوظة**: تحفظ تفضيلات المستخدم

## طريقة الاستخدام

### فتح إعدادات الصوت
```javascript
openUserSettings(); // يفتح نافذة إعدادات الصوت
```

### استخدام الأسماء المزخرفة
```javascript
openFancyNameEditor(); // يفتح محرر الأسماء المزخرفة
```

### إدارة المزارع
```javascript
// عرض حالة المزارع
showFarmStatus();

// إضافة مزرعة يدوية
addManualFarm("مزرعة القمح");

// إنشاء إشعار مخصص
createFarmAlert("مزرعة التجارب", "جاهزة للحصاد!", "success");
```

### الاختبار والتطوير
```javascript
// فحص حالة النظام
dev.check();

// اختبار إشعارات المزارع
dev.test();

// إضافة مزرعة تجريبية
dev.addFarm("مزرعة تجريبية");

// معاينة الأسماء المزخرفة
dev.fancy.showStylePreview("اسمك");
```

## عناصر واجهة المستخدم

### أزرار جديدة في شريط المستخدم
- **🌾 المزارع**: لعرض حالة المزارع
- **✨ اسم مزخرف**: لفتح محرر الأسماء

### نافذة حالة المزارع
- عدد المزارع الإجمالي
- المزارع النشطة
- الجاهزة للحصاد
- قيد البناء
- تحتاج عناية

### نافذة الأسماء المزخرفة
- إدخال النص الأصلي
- اختيار نوع الخط
- إضافة رمز زخرفي
- اختيار نوع الزخرفة
- معاينة فورية

## الملفات المعدلة

### JavaScript (`scripts/3d-game.js`)
- إضافة `FarmNotificationSystem` class
- إضافة `FancyNameSystem` class
- إضافة دوال التحكم والاختبار
- تكامل مع الأنظمة الموجودة

### HTML (`index-3d.html`)
- أزرار جديدة في شريط المستخدم
- نافذة عرض حالة المزارع
- ربط الأحداث مع الدوال الجديدة

### CSS (`styles/3d-game.css`)
- تصميم الأسماء المزخرفة
- تصميم نافذة حالة المزارع
- تصميم الأزرار الجديدة
- استجابة للشاشات الصغيرة

## المميزات التقنية

### الأداء
- فحص المزارع كل دقيقة فقط
- نظام انتظار لمنع الإشعارات المتكررة
- تحديث الواجهة كل 30 ثانية

### الأمان
- التحقق من وجود العناصر
- حماية من الأخطاء
- حفظ آمن للإعدادات

### قابلية التوسع
- نظام مرن لإضافة أنواع جديدة من المزارع
- سهولة إضافة خطوط وزخارف جديدة
- قابلية التخصيص الكاملة

## الأوامر التطويرية

في وحدة تحكم المتصفح، يمكن استخدام:
- `dev.check()` - فحص النظام
- `dev.test()` - اختبار الإشعارات
- `dev.farms` - الوصول لنظام المزارع
- `dev.fancy` - الوصول لنظام الأسماء

## ملاحظات مهمة

1. **التوافق**: يعمل النظام مع جميع المتصفحات الحديثة
2. **التخزين**: يحفظ البيانات في localStorage
3. **الأصوات**: يتطلب تفاعل المستخدم لتشغيل الموسيقى
4. **الأداء**: محسن للاستخدام على الأجهزة الضعيفة

---
*تم التطوير بواسطة MiniMax Agent*
*التاريخ: 2025-11-08*